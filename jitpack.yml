# https://docs.jitpack.io/building/
jdk:
  - openjdk17

before_install:
  - bash scripts/jitpack-setup.sh

install:
  - source "${HOME}/.jitpack-go-env.sh" && export GOTOOLCHAIN=auto && ./gradlew :libxray:publishReleasePublicationToMavenLocal --no-daemon --stacktrace
