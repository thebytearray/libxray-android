jdk:
  - openjdk17

before_install:

  - ./scripts/jitpack-setup.sh

install:
  - export GOTOOLCHAIN=auto PATH="/usr/local/go/bin:$PATH" && ./gradlew :libxray:publishReleasePublicationToMavenLocal --no-daemon --stacktrace
